<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.points.mapper.UserAddressMapper">

    <resultMap id="UserAddressResult" type="com.ruoyi.points.domain.UserAddress">
        <id     property="addressId"  column="address_id"/>
        <result property="userId"     column="user_id"/>
        <result property="consignee"  column="consignee"/>
        <result property="phone"      column="phone"/>
        <result property="province"   column="province"/>
        <result property="city"       column="city"/>
        <result property="district"   column="district"/>
        <result property="detail"     column="detail"/>
        <result property="isDefault"  column="is_default"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="addressVo">
        select address_id, user_id, consignee, phone, province, city, district, detail,
               is_default, create_time, update_time from t_user_address
    </sql>

    <select id="selectByUserId" resultMap="UserAddressResult" parameterType="Long">
        <include refid="addressVo"/> where user_id=#{userId} order by is_default desc, address_id desc
    </select>

    <select id="selectById" resultMap="UserAddressResult" parameterType="Long">
        <include refid="addressVo"/> where address_id=#{addressId}
    </select>

    <select id="selectDefault" resultMap="UserAddressResult" parameterType="Long">
        <include refid="addressVo"/> where user_id=#{userId} and is_default='1' limit 1
    </select>

    <insert id="insert" parameterType="com.ruoyi.points.domain.UserAddress" useGeneratedKeys="true" keyProperty="addressId">
        insert into t_user_address(user_id, consignee, phone, province, city, district, detail, is_default, create_time)
        values(#{userId}, #{consignee}, #{phone}, #{province}, #{city}, #{district}, #{detail},
               coalesce(#{isDefault},'0'), sysdate())
    </insert>

    <update id="update" parameterType="com.ruoyi.points.domain.UserAddress">
        update t_user_address
        <set>
            <if test="consignee != null">consignee=#{consignee},</if>
            <if test="phone != null">phone=#{phone},</if>
            <if test="province != null">province=#{province},</if>
            <if test="city != null">city=#{city},</if>
            <if test="district != null">district=#{district},</if>
            <if test="detail != null">detail=#{detail},</if>
            <if test="isDefault != null">is_default=#{isDefault},</if>
            update_time = sysdate()
        </set>
        where address_id=#{addressId} and user_id=#{userId}
    </update>

    <delete id="deleteById">
        delete from t_user_address where address_id=#{addressId} and user_id=#{userId}
    </delete>

    <update id="clearDefault" parameterType="Long">
        update t_user_address set is_default='0' where user_id=#{userId}
    </update>
</mapper>
